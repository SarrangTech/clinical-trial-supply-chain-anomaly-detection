"""Core pipeline package: synthetic data generators, WAP helper, GE validation,
Spark transforms, anomaly detection rules, and alerting -- imported directly by
Airflow DAGs and by pytest, so this package has no Airflow dependency itself.
"""
